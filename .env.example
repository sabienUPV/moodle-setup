# Change this to 'staging' or 'production' when deploying to the Ubuntu server
DEPLOY_ENV=local

# Base URL (e.g., http://localhost:8080 for local, https://learn.midominio.eu for staging/prod)
EXTERNAL_ROOT_URL=http://localhost:8080

# Database Credentials
DB_NAME=moodle_erasmus
DB_USER=moodle_user
DB_PASSWORD=moodle_secure_password
DB_ROOT_PASSWORD=moodle_root_secure